<%= form_for(@text_field) do |f| %>
  <% if @text_field.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@text_field.errors.count, "error") %> prohibited this contact_list from being saved:</h2>

      <ul>
      <% @text_field.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>

  <div class="actions">
    <%= f.submit button_name %>
  </div>
<% end %>